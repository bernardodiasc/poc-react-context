# useRoutesAndScreens Hook

## How to use

```
import useRoutesAndScreens from '@hooks/useRoutesAndScreens'

const SampleComponent = () => {
  const { anything } = useRoutesAndScreens()
  return (
    <div>{`Just ${anything} here`}</div>
  )
}
```
