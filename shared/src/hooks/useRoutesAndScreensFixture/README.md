# useRoutesAndScreensFixture Hook

## How to use

```
import useRoutesAndScreensFixture from '@hooks/useRoutesAndScreensFixture'

const SampleComponent = () => {
  const { anything } = useRoutesAndScreensFixture()
  return (
    <div>{`Just ${anything} here`}</div>
  )
}
```
