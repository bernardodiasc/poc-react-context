import { Meta, Story, Canvas } from '@storybook/addon-docs'

import LabelRow from './LabelRow'
import SwitchButton from '../SwitchButton'

<Meta title="DebugTools/LabelRow" component={LabelRow} />

<Canvas>
  <Story name="with SwitchButton">
    <LabelRow label="This is a label">
      <SwitchButton />
    </LabelRow>
  </Story>
  <Story name="with label">
    <LabelRow label="This is a label" />
  </Story>
  <Story name="no props">
    <LabelRow />
  </Story>
</Canvas>
